{
  "name": "material-retail",
  "version": "1.0.0",
  "private": true,
  "description": "Material Retail take-home project — inventory management + low-stock alerts for independent retailers.",
  "workspaces": [
    "apps/api",
    "apps/web"
  ],
  "scripts": {
    "dev": "npm-run-all --parallel dev:api dev:web",
    "dev:api": "npm --workspace apps/api run start:dev",
    "dev:web": "npm --workspace apps/web run dev",
    "build": "npm --workspace apps/api run build && npm --workspace apps/web run build",
    "start:api": "npm --workspace apps/api run start:prod",
    "preview:web": "npm --workspace apps/web run preview",
    "seed": "npm --workspace apps/api run seed",
    "smoke": "node apps/api/scripts/smoke.cjs",
    "lint:noop": "echo 'No linter configured for this 3-hour take-home.'"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
